## 1. 实现

- [x] 1.1 `page.tsx` 标题中 "Ripples." 的 `text-white/30` 改为 `text-ripple-400`。

## 2. 单元测试

- [x] 2.1 新增 `HomePage.test.tsx`，断言标题渲染且 "Ripples." 带 `text-ripple-400`（2 用例）。

## 3. E2E 验收

- [x] 3.1 新增 e2e 用例，断言 "Ripples." 为紫色系强调色而非低对比灰。
