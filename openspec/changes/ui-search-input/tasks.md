## 1. 实现

- [x] 1.1 `SearchBar.tsx` 静态输入框 `py-3` → `py-3.5`，边框 `border-white/[0.2]`，新增 hover/focus 反馈。

## 2. 单元测试

- [x] 2.1 新增 `SearchBar.test.tsx`，断言渲染出带占位符的搜索输入框（1 用例）。

## 3. E2E 验收

- [x] 3.1 新增 e2e 用例，断言搜索框高度 ≥48px、边框可见（非透明）。
