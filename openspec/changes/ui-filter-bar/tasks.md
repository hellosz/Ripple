## 1. 实现

- [x] 1.1 `SkillCardGrid.tsx` 分类标签 `gap-1.5` → `gap-2`。
- [x] 1.2 结果计数改为带"skills"标签的胶囊容器。
- [x] 1.3 筛选图标按钮加边框容器。

## 2. 单元测试

- [x] 2.1 新增 `SkillCardGrid.test.tsx`，断言结果计数"skills"标签与筛选按钮（2 用例）。

## 3. E2E 验收

- [x] 3.1 新增 e2e 用例，断言结果计数带"skills"标签、筛选按钮有边框。
