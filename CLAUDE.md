# CLAUDE.md

本仓库的系统规范与开发工作流统一维护在 **AGENTS.md**，请完整遵循：

@AGENTS.md

- 不要在本文件中新增规则；所有约定变更都改 `AGENTS.md`。
- 需求开发必须先走 OpenSpec 流程（`/opsx:propose` → validate → `/opsx:apply` → audit → archive），详见 AGENTS.md 第 4 节。
